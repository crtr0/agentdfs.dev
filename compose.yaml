services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: daily_fantasy_ai
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d daily_fantasy_ai"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:
